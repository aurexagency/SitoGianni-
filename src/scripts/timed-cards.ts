/**
 * timed-cards.ts — DISABILITATO
 * Le card sono ora sempre visibili in modalità list (statiche).
 * L'animazione GSAP Flip e l'auto-advance sono stati rimossi.
 */
export function initTimedCards(): void {
  // No-op: il layout è statico, tutte le card sono espanse.
}
